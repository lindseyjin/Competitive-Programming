#include <unordered_map>
#include <iostream>
using namespace std;

vector <vector <int> > returnpairs (vector <int> nums, int target) {
     unordered_map<int, boolean> pair;
    
    //add to unordered_map
    for (int x = 0; x < nums.size(); x++) {
        pair.insert(nums.at(x), false);
    }

    //iterate through map
    for (auto it = mymap.begin(); it != mymap.end(); ++it) {

    }

    
}

int main (int argc, char* argv[]) {
    
    return 0;
}